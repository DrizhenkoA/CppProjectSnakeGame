#include <iostream>
#include <string>
using namespace std;

struct Goods {
    string name;            //название
    int data;             //срок  годности ( в днях )
    int volume;             //объем продукта
    Goods** tovar = new Goods * [0];         //массив продуктов

};
struct Fridge {
	string name = "VOLGA";          //марка
	string volume;                 //объем
    int count = 0;                    //текущее кол-во еды в холодильнике

};
void goodInfo(Goods* tovar) {
    cout << "Name: " << tovar->name << endl;
    cout << "Data: " << tovar->data << endl;
    cout << "Volume: " << tovar->volume << endl;
    cout << "--------------------" << endl;
}
Goods* addGoods(string name, int data, int volume) {
    Goods* tovar = new Goods;
    tovar->name = name;
    tovar->data = data;
    tovar->volume = volume;
    return tovar;
}
void fridgeGoods(Fridge& fr, Goods* god) {

}
void showFridgeInfo(Fridge frid) {
    if (frid.count == 0) {
        cout << "FRIDGE IS EMPTY " << endl;
        return;
    }

}
void deleteGoods(Fridge& frid, string name) {
    int index = -1;
}


int main()
{
    setlocale(LC_ALL, "rus");
    Goods* gs1 = addGoods("Burger", 50, 200);
    Goods* gs2 = addGoods("Bread", 3, 600);
    Goods* gs3 = addGoods("Melon", 30, 3000);

    Fridge fr1;
    fr1.name = "VOLGA";
    fr1.volume = 200000;

}
